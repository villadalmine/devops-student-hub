# 🛠️ Prácticas, Laboratorios y Ejercicios

Guarda aquí tus proyectos de laboratorio, ejercicios prácticos y configuraciones:
- Archivos `Dockerfile` y `docker-compose.yml`.
- Manifiestos de Kubernetes (`deployment.yaml`, `service.yaml`).
- Scripts de Terraform (`main.tf`, `variables.tf`).
- Pipelines de CI/CD (`.github/workflows/ci.yml`).

Si encuentras algún error en tus archivos o comandos, pídele ayuda al Asistente IA seleccionando el **Modo Asistente de Terminal**.
